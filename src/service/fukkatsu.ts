import { json } from 'stream/consumers'
import { config } from './protobuf/config.proto'

export const shibou = (bf: Buffer): string => {
	return ''
}

export const fukkatsu = (str: string): Buffer => {
	return Buffer.from([])
}
